# Local fixture database only. Replace this one value when the data source changes.
PG_DSN=postgresql://report:report_local_only@localhost:55432/opinion_fixture

# OpenAI-compatible narrator settings. Keep real values in .env, never in Git.
LLM_BASE_URL=https://api.openai.com/v1
LLM_API_KEY=
LLM_MODEL=
